version: '3'
services:
  database:
    image: "postgres" # use latest official postgres version
    env_file:
      - database.env # configure postgres
    volumes:
      - task-managment-data:/var/lib/postgresql/data/ # persist data even if container shuts down

volumes:
  task-managment-data: # named volumes can be managed easier using docker-compose-data: # named volumes can be managed easier using docker-compose